import styled from "@emotion/styled";

function GomokuInfoCard() {
  return (
    <StyledGomokuInfoCard>
      <div>인포인포</div>
    </StyledGomokuInfoCard>
  );
}

export default GomokuInfoCard;

const StyledGomokuInfoCard = styled.main`
  padding: 8px;
  flex-grow: 1;
`;
